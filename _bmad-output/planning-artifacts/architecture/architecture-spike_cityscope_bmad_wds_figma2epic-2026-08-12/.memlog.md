---
scope: Spike Reno Calculator — 3-step renovation cost calculator + consented lead capture (React/MUI SPA + estimate/config/lead APIs)
purpose: build-substrate
altitude: feature
updated: 2026-08-12T16:56
---

- (version) Verified via npm registry 2026-08-12: next@16.3.0, react@19.2.8, @mui/material@9.3.1, @tanstack/react-query@5.101.4, zod@4.4.3, react-hook-form@7.85.0, typescript (current)
- (constraint) PRD NFR-4 fixes frontend: React + MUI (v5+); satisfied by MUI v9. NFR-5/6 require server-side seam for address API key + PII (cannot be client-only). NFR-9 requires config-driven items/questions.
- (decision) [ASSUMPTION] Paradigm: Ports & Adapters (Hexagonal) core behind a Next.js BFF; feature-sliced layered React/MUI client. Prevents: the 3 external unknowns (cost algo OI-3, address provider OI-6, lead sink OI-11) leaking into UI/domain and forcing rework when resolved.
- (decision) [ASSUMPTION] Stack/starter: Next.js 16 App Router (TypeScript) full-stack — hosts React 19 + MUI 9 client AND Route Handlers as the BFF/API. Prevents: divergent build/deploy/routing choices; gives one secure server boundary for keys+PII. Alternative weighed: Vite SPA + separate Node/Fastify API (more moving parts for a spike).
- (decision) Domain ports (server): EstimateEngine, AddressProvider, ConfigSource, LeadSink — each a port with a swappable adapter. Prevents: OI-3/OI-6/OI-1/OI-2/OI-11 blocking the build; deferred logic hides behind a stable interface.
- (decision) [ASSUMPTION] Validation: Zod schemas shared client+server as the single source of truth for form + API payload shapes (address, estimate request, lead). Prevents: client/server validation drift (FR-14/18/19/28).
- (decision) [ASSUMPTION] Client state: form state via react-hook-form; server/async state (address autocomplete, estimate, config) via TanStack Query. Prevents: two components managing the same async data incompatibly; centralizes loading/error/retry (FR-32/33).
- (decision) State ownership: RenovationEstimateForm is the single client-side aggregate for the in-progress flow (address, type, items, details); only it mutates flow state. estimateId returned by EstimateEngine is the join key a Lead references (FR-29). Prevents: two owners of scope/lead linkage.
- (decision) Money as integer AUD cents in domain+API; formatted to AUD string only at the view edge (NFR-10). Prevents: float rounding drift across estimate/results.
- (decision) BFF boundary: browser never calls third-party address/estimate/CRM directly — always via Next Route Handlers holding API keys server-side (NFR-5/6). Prevents: key leakage + CORS/consent bypass.
- (constraint) Deferred (owned by later work): real cost algorithm+pricing data (OI-3), address provider choice Google Places vs AusPost (OI-6), lead CRM/email/IVR sink (OI-11), final item/question config content (OI-1/OI-2), browser matrix (OI-12). All sit behind ports/config; do not block spine.
- (event) Reviewer Gate: lint PASS; 3 reviewers (versions/seams/rubric) all PASS-WITH-FINDINGS. Reviews in reviews/*.md.
- (version) Verified npm 2026-08-12: typescript@7.0.2, react-dom@19.2.8, @emotion/react@11.14.0, @emotion/styled@11.14.1; Node LTS v24 (Krypton). Next 16 needs Node>=20.9 -> target Node 24 LTS.
- (decision) Fix(seams+rubric): tighten AD-6 — server issues+validates estimateId; changing address or renovationType invalidates any prior EstimateResult (client discards estimateId, lead capture disabled until re-estimate); LeadSink rejects unknown/expired estimateId. Prevents stale/forged estimate-lead linkage.
- (decision) Fix(seams): tighten AD-8 — ConfigSource returns a versioned config (configVersion) with stable item/question ids; estimate request references item ids from that version; Step2/Step3 share id+shape conventions. Prevents cross-step/cross-adapter config shape divergence.
- (decision) Fix(seams): tighten AD-9 — error.fields is Record<fieldPath,string> (field->message), fieldPath = schema path. Prevents divergent field-error shapes.
- (decision) Fix(seams+NFR-8): add AD-12 — first-party typed analytics event seam (AnalyticsSink), fixed event enum (step_viewed/step_completed/estimate_generated/lead_submitted/drop_off), never carries PII. Gives NFR-8 + SM drop-off/conversion a home.
- (decision) Fix(rubric): NFR-3 performance home = per-feature code-splitting + TanStack Query caching + address debounce (convention); NFR-11 testing home = shared Zod schemas as contract + stub adapters make domain/use-cases unit-testable, E2E against stub adapters (convention).
- (event) Input reconciliation: rubric reviewer cross-checked PRD FR/NFR + features 4.1-4.7 against spine; only gaps were NFR-3/NFR-11 homes (now fixed). No PRD capability left without an architectural home.
- (event) Gate fixes applied: AD-6/AD-8/AD-9 tightened, AD-12 added, versions pinned (TS7/Node24/react-dom/emotion), MUI Grid + perf + testing conventions added. Lint re-run PASS 0 findings.
- (direction) Deferred (accepted): UX-behavioral seams OI-5/7/8/9/10 remain UX-owned — stories touching progression/state-matrix/address-reset/lead-placement must wait on bmad-ux outputs. Not architectural divergence.
- (direction) Deferred (accepted): operational envelope kept spike-light — single Next app, Node 24 host, dev/staging/prod, stub adapters in dev; deeper infra/observability out of scope at this altitude.
- (event) spine finalized
